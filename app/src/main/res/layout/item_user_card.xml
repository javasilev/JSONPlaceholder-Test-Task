<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView android:id="@+id/item_user_card"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/tiny"
    android:layout_marginLeft="@dimen/minor"
    android:layout_marginRight="@dimen/minor"
    android:layout_marginTop="@dimen/tiny"
    android:orientation="vertical"
    app:cardCornerRadius="2dp">
    
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="260dp"
        android:orientation="vertical"
        android:padding="@dimen/modest">
        
        <TextView
            android:id="@+id/item_user_card_text_view_caption"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/modest"
            android:paddingTop="@dimen/minor"
            android:textAppearance="@android:style/TextAppearance.DeviceDefault.Large"
            tools:text="Users" />
        
        <ListView
            android:id="@+id/item_user_card_list_view_user_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="false"
            android:divider="@android:color/transparent"
            android:dividerHeight="0dp"
            android:paddingBottom="@dimen/minor"
            android:scrollbars="none">
        
        </ListView>
    </LinearLayout>
</android.support.v7.widget.CardView>